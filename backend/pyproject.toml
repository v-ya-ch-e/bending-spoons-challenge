[project]
name = "bending-spoons-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "openai>=2.32.0",
    "python-dotenv",
    "uvicorn[standard]",
]

[tool.uv]
package = false
